"""
MODULE:

->  Module is just a single file containing code and we can use this file code in other file.
->  A single python file(.py)
->  Contains functions, variables, or classes.
->  Used to organize and reuse code
->  Python comes with lots of ready-to-use modules like:
    * math (for math operations)
    * random (for generating random numbers)
    * datetime (for date and time)
"""
import math

result = math.sqrt(16)
print(result)

"""
PACKAGE:

->  It is a folder that contains one or more modules (Python files). It may also contain sub-packages.
->  and you just have to use from and import keywords to use these things. 
->  There are 3rd party packages as well like Numpy, Pandas, Matplotlib etc., and we have to install all of these.
->  And we'll be learning all these and much more <3 
"""
