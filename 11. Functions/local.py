# local variable  :- These are only accessible inside the function scope and are not defined outside it.

def msg():
    choice = "I love you Shreya"   #variable declared locally inside the function and once function executes variable is gone
    print(choice)
    
msg()
