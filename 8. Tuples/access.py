a = (1,2,3)

"""
a[0] = 100
print(a)        Will give an error as the elements inside a tuple cannot be modified
"""

#slicing a tuple
print(a[0:3:1])

print(max(a))
print(min(a))

# These are used to store the data which does not require any change in the future
# for example:- DOB, Gender, UID